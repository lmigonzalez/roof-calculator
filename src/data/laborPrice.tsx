export const laborPrice = {
  Alabama: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Alaska: {
    min_price: 3.0,
    max_price: 4.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Arizona: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Arkansas: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  California: {
    min_price: 2.5,
    max_price: 4.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Colorado: {
    min_price: 2.0,
    max_price: 2.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Connecticut: {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Delaware: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Florida: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Georgia: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Hawaii: {
    min_price: 3.0,
    max_price: 4.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Idaho: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Illinois: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Indiana: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Iowa: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Kansas: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Kentucky: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Louisiana: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Maine: {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Maryland: {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Massachusetts: {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Michigan: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Minnesota: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Mississippi: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Missouri: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Montana: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Nebraska: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Nevada: {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'New Hampshire': {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'New Jersey': {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'New Mexico': {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'New York': {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'North Carolina': {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'North Dakota': {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Ohio: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Oklahoma: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Oregon: {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Pennsylvania: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'Rhode Island': {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'South Carolina': {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'South Dakota': {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Tennessee: {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Texas: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Utah: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Vermont: {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Virginia: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Washington: {
    min_price: 2.0,
    max_price: 3.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  'West Virginia': {
    min_price: 1.0,
    max_price: 1.5,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Wisconsin: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
  Wyoming: {
    min_price: 1.5,
    max_price: 2.0,
    average() {
      return (this.min_price + this.max_price) / 2;
    },
  },
};
